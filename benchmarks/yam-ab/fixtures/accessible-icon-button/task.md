# Task

Implement `renderDeleteButton()` in `src/delete-button.mjs`.

Return an icon-only delete button as an HTML string. It must remain
understandable to assistive technology without exposing the decorative SVG as
a second label. Keep the package manifest and tests unchanged.
