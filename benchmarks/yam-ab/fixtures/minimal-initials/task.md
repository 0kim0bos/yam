# Task

Implement `initials(name)` in `src/initials.mjs`.

Return up to two uppercase initials from a person's name. Treat repeated
whitespace as one separator and return an empty string for empty input. This is
a small project-specific transformation; keep the package and tests unchanged.
