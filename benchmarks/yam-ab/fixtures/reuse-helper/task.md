$quick

Implement `invoiceSummary(invoice)` in `src/invoice-summary.mjs`.

Requirements:

- Preserve the exported function name and arguments.
- Return `<invoice id>: <formatted total>`.
- `totalCents` is an integer number of cents.
- Follow the existing project pattern for formatting US dollars.
- Do not change `package.json`, `src/currency.mjs`, or the tests.
- Run `node --test`.
