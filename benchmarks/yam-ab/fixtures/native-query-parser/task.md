# Task

Implement `campaignSource(input)` in `src/campaign-source.mjs`.

It receives an absolute URL string and returns the decoded `utm_source` query
value, or an empty string when that parameter is absent. Keep the change
focused and do not modify tests or the package manifest.
