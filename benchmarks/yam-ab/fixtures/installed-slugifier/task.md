# Task

Implement `articleSlug(title)` in `src/article-slug.mjs`.

This project already ships an approved slugifier in `vendor/slugify.mjs`.
Return a stable URL slug while keeping the package manifest, vendor module, and
tests unchanged.
